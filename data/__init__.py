"""Data-loading helpers for the analysis CLI.

This package holds thin loaders that read the Parquet artifacts produced by the
download/verify/metrics pipeline back into DataFrames for plotting and tables.
The bulk generated data under this directory is git-ignored; only these source
modules are tracked.
"""
